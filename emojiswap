#!/usr/bin/env bash
# Thin wrapper: run emojiswap.py with the bundled venv (which has fonttools).
set -euo pipefail
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PY="$DIR/.venv/bin/python"
[ -x "$PY" ] || PY="python3"
exec "$PY" "$DIR/emojiswap.py" "$@"
